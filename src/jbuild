(jbuild_version 1)

(executables
 ((names (main))
  ; (c_names         (parser_flow))
  ; (c_flags (-I/_build/default/src/parser_flow.cmxa))
  ))
  
; DIR := _build/default/src
; PARSER_NAME := parser_flow.cmxa

;   CFLAGS := "-I $(PARSER_DIR)"
; LFLAGS := "-I $(PARSER_DIR) $(PARSER_NAME)"
